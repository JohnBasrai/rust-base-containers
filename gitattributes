# Treat all files as text and normalize line endings to LF
* text=auto eol=lf

# Ensure shell scripts are treated as UTF-8 text files
*.sh text eol=lf

# Explicitly set binary types
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
